<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
              https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

  <!-- Servlets -->
  <!-- Default page to serve -->
<welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>
  <listener>
  		  	<listener-class>scrobblefilter.web.ContextInitializer</listener-class>
  </listener>
  
  	<!--  spring servlet -->
		<servlet>
            <servlet-name>scrobblefilter</servlet-name>
            <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
            <load-on-startup>1</load-on-startup>
        </servlet>

         <servlet-mapping>
           <servlet-name>scrobblefilter</servlet-name>
          <url-pattern>/hello/*</url-pattern>
         </servlet-mapping>
         
 <security-role>
    <role-name>admin</role-name>
</security-role>
<security-role>
    <role-name>user</role-name>
</security-role>

  <!--
    /hello/admin/* and /hello/cron/* are gated programmatically in the controllers
    via X-Admin-Token / X-Cron-Token headers compared against MIGRATE_TOKEN /
    CRON_TOKEN env vars (sourced from Secret Manager on Cloud Run). The classic
    GAE security-constraint approach doesn't work on Cloud Run — Tomcat has no
    realm configured, so admin/cron requests would 403 unconditionally.
  -->
</web-app>
